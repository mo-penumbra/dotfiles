# This is Git's per-user configuration file.
[user]
	name = Mo
	email = mo@penumbra-ig.com
[alias]
	cleanup = "!git fetch -p && for branch in $(git branch -vv | grep ': gone]' | awk '{print $1}'); do git branch -d \"$branch\"; done"
